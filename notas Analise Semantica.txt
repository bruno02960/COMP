	
	
	-limitar numero de erros, ver branch
	
	
	-por booleano ou assim quando tem parenteses retos, para distinguir entre nomral variable e arrays


	so temos arrays de interiros e inteiros


	-A = h.var;   assumimos que ta noutro file, se A ja tiver declarado assumimos esse tipo, senao assumimos int


	-Boa ideia usar a ordem da arvore para gerar o codigo do JVM....depois o que foi gerado do precisa de ser invertido

	-Verificaçoes a fazer:
		-incializaçao
		-tipo para as operaçoes
		-ver se funçao existe
		-set variavel de retorno


	-para resolver o problema das funçoes e var poderem ter o mesmo nome,pode utilizar-se a classe symbo table que permite abstraçao e usa HashMap<String, Symbol>
	em que string é na realidade varaName-Type 
	
	-Cant set the size OF an array

	-Cant compare arrays
	 
	-All paths levam a var initialized, ter uma var no symbol em que se initailizada num if fica marcada dessa forma e se eles volta a por no estado normal
	
	-Slides yal
		Slide 5
		Último slide