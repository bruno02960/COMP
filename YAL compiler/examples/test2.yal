module program1 {
	data[]=[100]; // vector of 100 integers
	mx=4; // attribute mx
	a[]=[100];
	a[]=1;
	b=-7;
	mn=[a.size]; // attribute mn
	
	function det(d[]) {
        b = 1;
        c = 3;
		a[4]=9;
		z=[5];
		M=d.size-1; //d.size equivalent to d.length (Java)
		while(i<M) { // version not optimized!
			a=d[i]; i=i+1; b=d[i];
			mx= library1.max(a,3);
			mn= library1.min(a,b);
		}
	}
	
	function main() {
		det(data);
		io.println("max: ",mx);
		io.println("min: ",mn);
	}
}
